#include "../../incls/mini_rt.h"

// int	parse_shape(t_scene *scene, char *line, int i)
// {
// 	t_shape *forme;
//     t_sphere *sphere;

//     forme = ft_calloc(1, sizeof(t_shape));
//     sphere = ft_calloc(1, sizeof(t_sphere));

//     toutes parser la ligne pour la sphere

//     forme->shape = (void*)sphere;
//     forme->type = SPHERE;
//     vector_add_back_element(scene->shape, (void *)forme);
// 	return (0);
// }